/** TypeScript entry — Metro resolves `.native` / `.web` at bundle time. */
export {
  BG_REMOVAL_AVAILABLE,
  removeBackground,
  compositeOnWhite,
  type BgRemovalResult,
} from './bg-removal.web';
