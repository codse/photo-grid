/** TypeScript entry — Metro resolves `.native` / `.web` at bundle time. */
export { bakePersonPreview } from './person-preview.web';
export {
  PREVIEW_LONG_EDGE,
  previewOutputSize,
} from './person-preview-shared';
